#include<iostream>
using namespace std;

int sum(int,int);

int main()
{
	int a,b;
	cout<<"enter the two no:";
	cin>>a>>b;

	int r=sum(a,b);
    cout<<r;
}

int sum(int x,int y)
{
	int c=x + y;
	             //cout<<"sum is"<<c;
    return  c;
}
